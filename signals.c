#include "minishell.h"

void	start_signals(void)
{
	//| Iniciar os Signals
	/*
	 * SINAIS:
	 *
	 * - ctrl + D (deve dar 'exit').
	 * - ctrl + C (deve escrever '^C' no terminal).
	 * - ctrl + Z (?)
	 * - ctrl + '\' (deve fazer nada, nem escrever).
	*/
}
